# Backend server (172.28.92.57) — copy to .env.backend
# Used by: docker compose -f docker-compose.backend.yml --env-file .env.backend up -d

DB_PASSWORD=your_secure_dev_password
# Host port for PostgreSQL (frontend will connect to 172.28.92.57:5002)
DB_PORT=5002
